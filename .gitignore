# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
/logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
!.env.*.example

# Docker 部署 env 文件（实际配置不入库，仅 .example 模板入库）
# 下方显式列出以明示意并保持对称（已被上方 .env.* 通配符覆盖，此处补全 docker/env 与 server/db 路径）
docker/env/.env.app
docker/env/.env.migrate
docker/env/.env.mysql
docker/env/.env.redis
server/db/.env.migrate

# pm2 本机配置（样例 ecosystem.config.example.cjs 入库，真实配置不入库）
ecosystem.config.cjs

content/
.uploads/
public/audio/
.trae/
.qoder/
.agents/
.claude/
AGENTS.md

# Vitest 临时配置文件
vitest.config.ts.timestamp-*.mjs
auto-imports.d.ts
public/uploads/

# 迁移 SQL 本地备份（占位符化前的原始快照，仅本地留存不入库）
*.sql.bak
# 忽略所有备份文件
*.bak
