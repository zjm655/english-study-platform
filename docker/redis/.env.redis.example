# Redis 容器独立配置（env_file 注入；模板入库，.env.redis 被 gitignore）
# 复制使用：cp .env.redis.example .env.redis

# Redis 访问密码（唯一必填，请用强密码）：--requirepass 由容器内 shell 展开注入，不入库
REDIS_PASSWORD=test123

# 可选：内存上限（默认 256mb，已写死于 conf/redis.conf；此处仅占位不参与生效）
#REDIS_MAXMEMORY=256mb
