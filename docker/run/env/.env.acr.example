# ====================================================================
# ACR 构建/推送配置（复制为 .env.acr；含密码不入库）。用法：
#   1) docker login --username=ACR_USERNAME ACR_REGISTRY   # 服务器拉私有镜像前执行一次
#   2) 构建推送（构建机）：
#      docker build --target runner -t $ACR_REGISTRY/$ACR_NAMESPACE/english-platform:$IMAGE_TAG .
#      docker push $ACR_REGISTRY/$ACR_NAMESPACE/english-platform:$IMAGE_TAG
# 注意：compose 运行需 --env-file ./env/.env.acr 加载（run/docker-compose.yml 的 image 地址变量化）；
#       无机器 IP 变量（已解耦）。
# ====================================================================
ACR_REGISTRY=""    # ACR 个人版域名（控制台复制，新 crpi 格式）
ACR_NAMESPACE=""   # 命名空间（如 aliyun-docker-zjm655）
ACR_USERNAME=""    # 登录用户名（访问凭证页）
ACR_PASSWORD=""    # 访问凭证固定密码（非阿里云账号密码）
IMAGE_TAG=""       # 镜像 tag（每次发版指定；compose 运行时必填）
